<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quyền vẽ overlay nổi trên các app khác (bắt buộc, phải xin runtime) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service để overlay + booster sống bền khi app ở nền -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Thông báo bắt buộc cho foreground service trên Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Dọn app chạy nền để tăng RAM khả dụng khi chơi game -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Ghi cấu hình hệ thống (khóa độ sáng) - cần xin quyền đặc biệt runtime -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Giữ màn hình sáng khi chơi game -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Vô hiệu hóa/khôi phục Không làm phiền khi vào/ra chế độ Booster -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameBooster"
        android:name=".GameBoosterApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service nổi: vẽ vùng cố định + chạy booster nền -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="game_overlay_and_performance_boost" />
        </service>

    </application>

</manifest>
